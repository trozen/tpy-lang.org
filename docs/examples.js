const EXAMPLES = [{"label":"mandelbrot","file":"mandelbrot.py","src":"# Many ordinary Python programs run as-is under\n# TurboPython -- compiled native, often more than\n# an order of magnitude faster than CPython.\nWIDTH, HEIGHT, MAX_ITER = 64, 28, 90\nCHARS = \" .:-=+*#%@\"\n\ndef escape(cx: float, cy: float) -> int:\n    x, y = 0.0, 0.0\n    for i in range(MAX_ITER):\n        if x * x + y * y > 4.0:\n            return i\n        x, y = x * x - y * y + cx, 2.0 * x * y + cy\n    return MAX_ITER\n\ndef main() -> None:\n    for row in range(HEIGHT):\n        cy = -1.2 + 2.4 * row / HEIGHT\n        for col in range(WIDTH):\n            cx = -2.4 + 3.2 * col / WIDTH\n            idx = escape(cx, cy) % len(CHARS)\n            print(CHARS[idx], end=\"\")\n        print(\"\")\n\nmain()\n","code":"<span class=\"c\"># Many ordinary Python programs run as-is under</span>\n<span class=\"c\"># TurboPython -- compiled native, often more than</span>\n<span class=\"c\"># an order of magnitude faster than CPython.</span>\nWIDTH, HEIGHT, MAX_ITER = <span class=\"num\">64</span>, <span class=\"num\">28</span>, <span class=\"num\">90</span>\nCHARS = <span class=\"s\">\" .:-=+*#%@\"</span>\n\n<span class=\"k\">def</span> <span class=\"fn\">escape</span>(cx: <span class=\"fn\">float</span>, cy: <span class=\"fn\">float</span>) -&gt; <span class=\"fn\">int</span>:\n    x, y = <span class=\"num\">0.0</span>, <span class=\"num\">0.0</span>\n    <span class=\"k\">for</span> i <span class=\"k\">in</span> <span class=\"fn\">range</span>(MAX_ITER):\n        <span class=\"k\">if</span> x * x + y * y &gt; <span class=\"num\">4.0</span>:\n            <span class=\"k\">return</span> i\n        x, y = x * x - y * y + cx, <span class=\"num\">2.0</span> * x * y + cy\n    <span class=\"k\">return</span> MAX_ITER\n\n<span class=\"k\">def</span> <span class=\"fn\">main</span>() -&gt; <span class=\"k\">None</span>:\n    <span class=\"k\">for</span> row <span class=\"k\">in</span> <span class=\"fn\">range</span>(HEIGHT):\n        cy = -<span class=\"num\">1.2</span> + <span class=\"num\">2.4</span> * row / HEIGHT\n        <span class=\"k\">for</span> col <span class=\"k\">in</span> <span class=\"fn\">range</span>(WIDTH):\n            cx = -<span class=\"num\">2.4</span> + <span class=\"num\">3.2</span> * col / WIDTH\n            idx = <span class=\"fn\">escape</span>(cx, cy) % <span class=\"fn\">len</span>(CHARS)\n            <span class=\"fn\">print</span>(CHARS[idx], end=<span class=\"s\">\"\"</span>)\n        <span class=\"fn\">print</span>(<span class=\"s\">\"\"</span>)\n\n<span class=\"fn\">main</span>()"},{"label":"standard types","file":"standard_types.py","src":"# Built-in types and idioms. An unannotated integer\n# literal is Int32; `int` is arbitrary-precision.\ndef main() -> None:\n    n = 6 * 7              # Int32 (default integer type)\n    big: int = 2 ** 40     # int -> arbitrary precision int\n    ratio = 22 / 7         # float\n    ok = n > 40            # bool\n    name = \"TurboPython\"   # str\n    raw = b\"\\x01\\x02\\x03\"  # bytes\n    point = (3, 4)         # tuple\n\n    nums = [5, 3, 8, 1, 9]\n    evens = [x for x in nums if x % 2 == 0]   # list comp\n    squares = {x: x * x for x in evens}       # dict comp\n    thirds = {x % 3 for x in nums}            # set comp\n\n    first, last = nums[0], nums[-1]   # negative index\n    head = nums[:3]                   # slicing\n    maybe: int | None = None          # Optional\n\n    print(f\"int32={n} big={big} ok={ok}\")\n    print(\"float:\", ratio)\n    print(\"str:\", name.lower(), \"bytes:\", len(raw), raw[0])\n    print(\"tuple:\", point, \"ends:\", first, last)\n    print(\"evens:\", evens, \"head:\", head)\n    print(\"squares:\", squares, \"thirds:\", sorted(thirds))\n    print(\"none?\", maybe is None, \"equals:\", maybe)\n\nmain()\n","code":"<span class=\"c\"># Built-in types and idioms. An unannotated integer</span>\n<span class=\"c\"># literal is Int32; `int` is arbitrary-precision.</span>\n<span class=\"k\">def</span> <span class=\"fn\">main</span>() -&gt; <span class=\"k\">None</span>:\n    n = <span class=\"num\">6</span> * <span class=\"num\">7</span>              <span class=\"c\"># Int32 (default integer type)</span>\n    big: <span class=\"fn\">int</span> = <span class=\"num\">2</span> ** <span class=\"num\">40</span>     <span class=\"c\"># int -&gt; arbitrary precision int</span>\n    ratio = <span class=\"num\">22</span> / <span class=\"num\">7</span>         <span class=\"c\"># float</span>\n    ok = n &gt; <span class=\"num\">40</span>            <span class=\"c\"># bool</span>\n    name = <span class=\"s\">\"TurboPython\"</span>   <span class=\"c\"># str</span>\n    raw = <span class=\"s\">b\"\\x01\\x02\\x03\"</span>  <span class=\"c\"># bytes</span>\n    point = (<span class=\"num\">3</span>, <span class=\"num\">4</span>)         <span class=\"c\"># tuple</span>\n\n    nums = [<span class=\"num\">5</span>, <span class=\"num\">3</span>, <span class=\"num\">8</span>, <span class=\"num\">1</span>, <span class=\"num\">9</span>]\n    evens = [x <span class=\"k\">for</span> x <span class=\"k\">in</span> nums <span class=\"k\">if</span> x % <span class=\"num\">2</span> == <span class=\"num\">0</span>]   <span class=\"c\"># list comp</span>\n    squares = {x: x * x <span class=\"k\">for</span> x <span class=\"k\">in</span> evens}       <span class=\"c\"># dict comp</span>\n    thirds = {x % <span class=\"num\">3</span> <span class=\"k\">for</span> x <span class=\"k\">in</span> nums}            <span class=\"c\"># set comp</span>\n\n    first, last = nums[<span class=\"num\">0</span>], nums[-<span class=\"num\">1</span>]   <span class=\"c\"># negative index</span>\n    head = nums[:<span class=\"num\">3</span>]                   <span class=\"c\"># slicing</span>\n    maybe: <span class=\"fn\">int</span> | <span class=\"k\">None</span> = <span class=\"k\">None</span>          <span class=\"c\"># Optional</span>\n\n    <span class=\"fn\">print</span>(f\"int32={n} big={big} ok={ok}\")\n    <span class=\"fn\">print</span>(<span class=\"s\">\"float:\"</span>, ratio)\n    <span class=\"fn\">print</span>(<span class=\"s\">\"str:\"</span>, name.<span class=\"fn\">lower</span>(), <span class=\"s\">\"bytes:\"</span>, <span class=\"fn\">len</span>(raw), raw[<span class=\"num\">0</span>])\n    <span class=\"fn\">print</span>(<span class=\"s\">\"tuple:\"</span>, point, <span class=\"s\">\"ends:\"</span>, first, last)\n    <span class=\"fn\">print</span>(<span class=\"s\">\"evens:\"</span>, evens, <span class=\"s\">\"head:\"</span>, head)\n    <span class=\"fn\">print</span>(<span class=\"s\">\"squares:\"</span>, squares, <span class=\"s\">\"thirds:\"</span>, <span class=\"fn\">sorted</span>(thirds))\n    <span class=\"fn\">print</span>(<span class=\"s\">\"none?\"</span>, maybe <span class=\"k\">is</span> <span class=\"k\">None</span>, <span class=\"s\">\"equals:\"</span>, maybe)\n\n<span class=\"fn\">main</span>()"},{"label":"stdlib","file":"stdlib_demo.py","src":"# Many standard-library modules work out of the box.\nimport re\nfrom argparse import ArgumentParser\nfrom dataclasses import dataclass\n\n@dataclass\nclass Address:\n    user: str\n    host: str\n\n@dataclass\nclass Contact:\n    id: int\n    name: str\n    address: Address\n\ndef main() -> None:\n    parser = ArgumentParser(description=\"Add a contact.\")\n    parser.add_argument(\"--id\", type=int, default=1)\n    parser.add_argument(\"--name\", default=\"Ada\")\n    parser.add_argument(\"--email\", default=\"ada@acme.io\")\n    parser.add_argument(\"-v\", \"--verbose\",\n                        action=\"store_true\")\n    args = parser.parse_args()\n\n    m = re.match(r\"(\\w+)@([\\w.]+)\", args.email)\n    if m is not None:\n        addr = Address(m.group(1), m.group(2))\n        c = Contact(args.id, args.name, addr)\n        print(c)\n        if args.verbose:\n            print(\"host:\", c.address.host)\n\nmain()\n","code":"<span class=\"c\"># Many standard-library modules work out of the box.</span>\n<span class=\"k\">import</span> re\n<span class=\"k\">from</span> argparse <span class=\"k\">import</span> ArgumentParser\n<span class=\"k\">from</span> dataclasses <span class=\"k\">import</span> dataclass\n\n@dataclass\n<span class=\"k\">class</span> Address:\n    user: <span class=\"fn\">str</span>\n    host: <span class=\"fn\">str</span>\n\n@dataclass\n<span class=\"k\">class</span> Contact:\n    id: <span class=\"fn\">int</span>\n    name: <span class=\"fn\">str</span>\n    address: Address\n\n<span class=\"k\">def</span> <span class=\"fn\">main</span>() -&gt; <span class=\"k\">None</span>:\n    parser = <span class=\"fn\">ArgumentParser</span>(description=<span class=\"s\">\"Add a contact.\"</span>)\n    parser.<span class=\"fn\">add_argument</span>(<span class=\"s\">\"--id\"</span>, type=<span class=\"fn\">int</span>, default=<span class=\"num\">1</span>)\n    parser.<span class=\"fn\">add_argument</span>(<span class=\"s\">\"--name\"</span>, default=<span class=\"s\">\"Ada\"</span>)\n    parser.<span class=\"fn\">add_argument</span>(<span class=\"s\">\"--email\"</span>, default=<span class=\"s\">\"ada@acme.io\"</span>)\n    parser.<span class=\"fn\">add_argument</span>(<span class=\"s\">\"-v\"</span>, <span class=\"s\">\"--verbose\"</span>,\n                        action=<span class=\"s\">\"store_true\"</span>)\n    args = parser.<span class=\"fn\">parse_args</span>()\n\n    m = re.<span class=\"fn\">match</span>(<span class=\"s\">r\"(\\w+)@([\\w.]+)\"</span>, args.email)\n    <span class=\"k\">if</span> m <span class=\"k\">is</span> <span class=\"k\">not</span> <span class=\"k\">None</span>:\n        addr = <span class=\"fn\">Address</span>(m.<span class=\"fn\">group</span>(<span class=\"num\">1</span>), m.<span class=\"fn\">group</span>(<span class=\"num\">2</span>))\n        c = <span class=\"fn\">Contact</span>(args.id, args.name, addr)\n        <span class=\"fn\">print</span>(c)\n        <span class=\"k\">if</span> args.verbose:\n            <span class=\"fn\">print</span>(<span class=\"s\">\"host:\"</span>, c.address.host)\n\n<span class=\"fn\">main</span>()"},{"label":"classes","file":"classes.py","src":"# Value types (int, float, bool, str) are copied. Reference\n# types (classes, list, dict, set) are shared: a name or\n# parameter refers to the same object.\nclass Account:\n    def __init__(self, owner: str, balance: int) -> None:\n        self.owner = owner\n        self.balance = balance\n\n    def deposit(self, amount: int) -> None:\n        self.balance += amount\n\ndef topup(acc: Account, amount: int) -> None:\n    acc.balance += amount\n\ndef main() -> None:\n    a = Account(\"alice\", 100)\n    b = a                      # b is the same account\n    b.deposit(50)              # change it through b...\n    topup(a, 30)               # ...or through a function\n    print(a.owner, a.balance)  # a sees both: alice 180\n\n    x = 100\n    y = x                      # y is an independent copy\n    y += 1\n    print(\"x:\", x, \"y:\", y)    # x: 100 y: 101\n\nmain()\n","code":"<span class=\"c\"># Value types (int, float, bool, str) are copied. Reference</span>\n<span class=\"c\"># types (classes, list, dict, set) are shared: a name or</span>\n<span class=\"c\"># parameter refers to the same object.</span>\n<span class=\"k\">class</span> Account:\n    <span class=\"k\">def</span> <span class=\"fn\">__init__</span>(self, owner: <span class=\"fn\">str</span>, balance: <span class=\"fn\">int</span>) -&gt; <span class=\"k\">None</span>:\n        self.owner = owner\n        self.balance = balance\n\n    <span class=\"k\">def</span> <span class=\"fn\">deposit</span>(self, amount: <span class=\"fn\">int</span>) -&gt; <span class=\"k\">None</span>:\n        self.balance += amount\n\n<span class=\"k\">def</span> <span class=\"fn\">topup</span>(acc: Account, amount: <span class=\"fn\">int</span>) -&gt; <span class=\"k\">None</span>:\n    acc.balance += amount\n\n<span class=\"k\">def</span> <span class=\"fn\">main</span>() -&gt; <span class=\"k\">None</span>:\n    a = <span class=\"fn\">Account</span>(<span class=\"s\">\"alice\"</span>, <span class=\"num\">100</span>)\n    b = a                      <span class=\"c\"># b is the same account</span>\n    b.<span class=\"fn\">deposit</span>(<span class=\"num\">50</span>)              <span class=\"c\"># change it through b...</span>\n    <span class=\"fn\">topup</span>(a, <span class=\"num\">30</span>)               <span class=\"c\"># ...or through a function</span>\n    <span class=\"fn\">print</span>(a.owner, a.balance)  <span class=\"c\"># a sees both: alice 180</span>\n\n    x = <span class=\"num\">100</span>\n    y = x                      <span class=\"c\"># y is an independent copy</span>\n    y += <span class=\"num\">1</span>\n    <span class=\"fn\">print</span>(<span class=\"s\">\"x:\"</span>, x, <span class=\"s\">\"y:\"</span>, y)    <span class=\"c\"># x: 100 y: 101</span>\n\n<span class=\"fn\">main</span>()"},{"label":"ownership","file":"ownership.py","src":"# Reference types are borrowed at parameters and returns:\n# no copy, the caller keeps ownership. Own[T] transfers\n# ownership -- use it when a function returns a value or\n# keeps one past the call.\n# tpy is the module for core TurboPython types\nfrom tpy import Own\n\nglobal_shelf: list[list[int]] = []\n\n# just reads xs -- borrowing is enough\ndef total(xs: list[int]) -> int:\n    return sum(xs)\n\n# returns a new list -- Own hands the caller ownership\ndef squares(n: int) -> Own[list[int]]:\n    return [i * i for i in range(n)]\n\n# keeps xs after the call -- Own takes ownership\ndef store(xs: Own[list[int]]) -> None:\n    global_shelf.append(xs)\n\ndef main() -> None:\n    nums = squares(5)        # nums owns the new list\n    print(\"total:\", total(nums))  # borrowed, not moved\n    store(nums)              # ownership moves out\n    print(\"shelf:\", global_shelf[0])\n\nmain()\n","code":"<span class=\"c\"># Reference types are borrowed at parameters and returns:</span>\n<span class=\"c\"># no copy, the caller keeps ownership. Own[T] transfers</span>\n<span class=\"c\"># ownership -- use it when a function returns a value or</span>\n<span class=\"c\"># keeps one past the call.</span>\n<span class=\"c\"># tpy is the module for core TurboPython types</span>\n<span class=\"k\">from</span> tpy <span class=\"k\">import</span> Own\n\nglobal_shelf: list[list[<span class=\"fn\">int</span>]] = []\n\n<span class=\"c\"># just reads xs -- borrowing is enough</span>\n<span class=\"k\">def</span> <span class=\"fn\">total</span>(xs: list[<span class=\"fn\">int</span>]) -&gt; <span class=\"fn\">int</span>:\n    <span class=\"k\">return</span> <span class=\"fn\">sum</span>(xs)\n\n<span class=\"c\"># returns a new list -- Own hands the caller ownership</span>\n<span class=\"k\">def</span> <span class=\"fn\">squares</span>(n: <span class=\"fn\">int</span>) -&gt; Own[list[<span class=\"fn\">int</span>]]:\n    <span class=\"k\">return</span> [i * i <span class=\"k\">for</span> i <span class=\"k\">in</span> <span class=\"fn\">range</span>(n)]\n\n<span class=\"c\"># keeps xs after the call -- Own takes ownership</span>\n<span class=\"k\">def</span> <span class=\"fn\">store</span>(xs: Own[list[<span class=\"fn\">int</span>]]) -&gt; <span class=\"k\">None</span>:\n    global_shelf.<span class=\"fn\">append</span>(xs)\n\n<span class=\"k\">def</span> <span class=\"fn\">main</span>() -&gt; <span class=\"k\">None</span>:\n    nums = <span class=\"fn\">squares</span>(<span class=\"num\">5</span>)        <span class=\"c\"># nums owns the new list</span>\n    <span class=\"fn\">print</span>(<span class=\"s\">\"total:\"</span>, <span class=\"fn\">total</span>(nums))  <span class=\"c\"># borrowed, not moved</span>\n    <span class=\"fn\">store</span>(nums)              <span class=\"c\"># ownership moves out</span>\n    <span class=\"fn\">print</span>(<span class=\"s\">\"shelf:\"</span>, global_shelf[<span class=\"num\">0</span>])\n\n<span class=\"fn\">main</span>()"},{"label":"requests + json","file":"requests_demo.py","src":"# tplib is TurboPython's standard library: packages for\n# common tasks, built for a statically compiled language.\nimport tplib.requests as requests\nfrom tplib.json.model import model, field\n\n@model  # inspired by pydantic\nclass Repo:\n    full_name: str\n    description: str\n    language: str\n    stars: int = field(alias=\"stargazers_count\")\n    forks: int = field(alias=\"forks_count\")\n\ndef main() -> None:\n    url = \"https://api.github.com/repos/python/cpython\"\n    # inspired by requests, a popular Python package\n    repo = Repo.from_json(requests.get(url).text)\n    print(f\"{repo.full_name} -- {repo.description}\")\n    print(f\"{repo.language}: {repo.stars} stars, {repo.forks} forks\")\n\nmain()\n","code":"<span class=\"c\"># tplib is TurboPython's standard library: packages for</span>\n<span class=\"c\"># common tasks, built for a statically compiled language.</span>\n<span class=\"k\">import</span> tplib.requests <span class=\"k\">as</span> requests\n<span class=\"k\">from</span> tplib.json.model <span class=\"k\">import</span> model, field\n\n@model  <span class=\"c\"># inspired by pydantic</span>\n<span class=\"k\">class</span> Repo:\n    full_name: <span class=\"fn\">str</span>\n    description: <span class=\"fn\">str</span>\n    language: <span class=\"fn\">str</span>\n    stars: <span class=\"fn\">int</span> = <span class=\"fn\">field</span>(alias=<span class=\"s\">\"stargazers_count\"</span>)\n    forks: <span class=\"fn\">int</span> = <span class=\"fn\">field</span>(alias=<span class=\"s\">\"forks_count\"</span>)\n\n<span class=\"k\">def</span> <span class=\"fn\">main</span>() -&gt; <span class=\"k\">None</span>:\n    url = <span class=\"s\">\"https://api.github.com/repos/python/cpython\"</span>\n    <span class=\"c\"># inspired by requests, a popular Python package</span>\n    repo = Repo.<span class=\"fn\">from_json</span>(requests.<span class=\"fn\">get</span>(url).text)\n    <span class=\"fn\">print</span>(f\"{repo.full_name} -- {repo.description}\")\n    <span class=\"fn\">print</span>(f\"{repo.language}: {repo.stars} stars, {repo.forks} forks\")\n\n<span class=\"fn\">main</span>()"},{"label":"async","file":"async_demo.py","src":"# A tiny async HTTP server. Run it, then open\n# http://127.0.0.1:8080 in a browser or curl it.\n# Stop it with Ctrl-C.\nimport asyncio\nimport time\nfrom asyncio import StreamReader, StreamWriter\nfrom tpy import Own\n\nasync def handle(reader: Own[StreamReader],\n                 writer: Own[StreamWriter]) -> None:\n    await reader.readline()         # the request line\n    now = int(time.time())\n    writer.write(b\"HTTP/1.1 200 OK\\r\\n\")\n    writer.write(b\"Content-Type: text/html\\r\\n\")\n    writer.write(b\"Connection: close\\r\\n\\r\\n\")\n    writer.write(b\"<h1>Hello from TurboPython</h1>\")\n    writer.write(f\"<p>epoch time: {now}</p>\".encode())\n    await writer.drain()\n    writer.close()\n\nasync def serve() -> None:\n    addr = \"127.0.0.1\"\n    port = 8080\n    server = await asyncio.start_server(\n        handle, addr, port)\n    print(f\"serving on http://{addr}:{port} ...\")\n    await server.serve_forever()\n\ndef main() -> None:\n    try:\n        asyncio.run(serve())\n    except KeyboardInterrupt:\n        print(\"shutting down\")\n\nmain()\n","code":"<span class=\"c\"># A tiny async HTTP server. Run it, then open</span>\n<span class=\"c\"># http://127.0.0.1:8080 in a browser or curl it.</span>\n<span class=\"c\"># Stop it with Ctrl-C.</span>\n<span class=\"k\">import</span> asyncio\n<span class=\"k\">import</span> time\n<span class=\"k\">from</span> asyncio <span class=\"k\">import</span> StreamReader, StreamWriter\n<span class=\"k\">from</span> tpy <span class=\"k\">import</span> Own\n\n<span class=\"k\">async</span> <span class=\"k\">def</span> <span class=\"fn\">handle</span>(reader: Own[StreamReader],\n                 writer: Own[StreamWriter]) -&gt; <span class=\"k\">None</span>:\n    <span class=\"k\">await</span> reader.<span class=\"fn\">readline</span>()         <span class=\"c\"># the request line</span>\n    now = <span class=\"fn\">int</span>(time.<span class=\"fn\">time</span>())\n    writer.<span class=\"fn\">write</span>(<span class=\"s\">b\"HTTP/1.1 200 OK\\r\\n\"</span>)\n    writer.<span class=\"fn\">write</span>(<span class=\"s\">b\"Content-Type: text/html\\r\\n\"</span>)\n    writer.<span class=\"fn\">write</span>(<span class=\"s\">b\"Connection: close\\r\\n\\r\\n\"</span>)\n    writer.<span class=\"fn\">write</span>(<span class=\"s\">b\"&lt;h1&gt;Hello from TurboPython&lt;/h1&gt;\"</span>)\n    writer.<span class=\"fn\">write</span>(f\"&lt;p&gt;epoch time: {now}&lt;/p&gt;\".<span class=\"fn\">encode</span>())\n    <span class=\"k\">await</span> writer.<span class=\"fn\">drain</span>()\n    writer.<span class=\"fn\">close</span>()\n\n<span class=\"k\">async</span> <span class=\"k\">def</span> <span class=\"fn\">serve</span>() -&gt; <span class=\"k\">None</span>:\n    addr = <span class=\"s\">\"127.0.0.1\"</span>\n    port = <span class=\"num\">8080</span>\n    server = <span class=\"k\">await</span> asyncio.<span class=\"fn\">start_server</span>(\n        handle, addr, port)\n    <span class=\"fn\">print</span>(f\"serving on http://{addr}:{port} ...\")\n    <span class=\"k\">await</span> server.<span class=\"fn\">serve_forever</span>()\n\n<span class=\"k\">def</span> <span class=\"fn\">main</span>() -&gt; <span class=\"k\">None</span>:\n    <span class=\"k\">try</span>:\n        asyncio.<span class=\"fn\">run</span>(<span class=\"fn\">serve</span>())\n    <span class=\"k\">except</span> KeyboardInterrupt:\n        <span class=\"fn\">print</span>(<span class=\"s\">\"shutting down\"</span>)\n\n<span class=\"fn\">main</span>()"}];
